<div class="mb-5.5 flex flex-col gap-5.5 sm:flex-row">
    <div class="w-full sm:w-1/2">
        <InputGroup
            id="{{ name }}"
            :label="$t('form.{{ name }}')"
            type="text"
            :placeholder="$t('form.{{ name }}')"
            v-model="form.{{ name }}">
        </InputGroup>
        <form-error :errors="validationErrors" field="{{ name }}"></form-error>
    </div>
</div>